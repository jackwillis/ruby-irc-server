# frozen_string_literal: true

source 'https://rubygems.org'

gem 'concurrent-ruby', '1.2.2', require: 'concurrent'
gem 'concurrent-ruby-ext', '1.2.2', platform: :mri

group :development do
    gem 'pry', '~> 0.14'
end

group :testing do
    gem 'rspec', '~> 3.12'
end
